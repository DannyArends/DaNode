language: d
sudo: true
before_install:
  - sudo apt-get install -y libssl-dev
addons:
  apt:
    update: true
matrix:
  include:
    - d: 'dmd'
      env: CONFIG=default
  allow_failures:
    - d: 'dmd'
      env: CONFIG=ssl
script: dub test -c $CONFIG
